const { MODAL_CONFIGS, addHistory, handleModal } = require('./modal');
const { handleRoleButton } = require('./roleButton');

module.exports = {
  MODAL_CONFIGS,
  addHistory,
  handleModal,
  handleRoleButton,
};
