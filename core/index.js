const warnings = require('./warnings');
const config = require('./config');
const embeds = require('./embeds');
const autoAction = require('./autoAction');
const permissions = require('./permissions');

module.exports = {
  warnings,
  config,
  embeds,
  autoAction,
  permissions,
};
